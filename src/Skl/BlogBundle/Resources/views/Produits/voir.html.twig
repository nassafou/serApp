{% extends "BlogBundle::layout.html.twig" %}
{% block title %}
Lecture d'un article - {{ parent() }}
{% endblock %}
{% block blog_body %}
<h2>{{ article.nom }}</h2>
<i> Prix {{ article.Prix }}, Poids{{ article.poids }}</i>
<div class="well">
{{ article.image.url }}
</div>
<div>
    {{ article.description }}
</div>

<p>
<a href="{{ path('accueil') }}" class="btn">
<i class="icon-chevron-left"></i>Retour à la liste </a>
        <a href="{{ path('article_modifier', {'id': article.id}) }}" class="btn">
        <i class="icon-edit"></i>Modifier l'article</a>
       <a href="{{ path('article_supprimer', {'id': article.id}) }}" class="btn">
       <i class="icon-trash"></i>Supprimer l'article</a>
</p>
{% endblock %}